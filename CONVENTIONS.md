# 开发约定 — 所有子 skill 必须遵守

本文件是本技能包的「宪法」。新增或修改任何子 skill 前先读这里，再读范本
`skills/grant-proposal/SKILL.md`。

## 1. vendor-first

实现任何能力前，先查 `vendor/` 是否已有底座（见 `vendor/VENDOR.md` 溯源表）。
**有则引用，禁止重写。** 引用时在 SKILL.md 里写明：

```markdown
## 底座
- `vendor/awesome-benzi/references/QUALITY_GATES.md` — 质量门规则，直接复用
- `vendor/office-layer/` — 文件产出通道
```

## 2. 文件产出唯一通道

任何 docx / pptx / xlsx / pdf 产出**一律**走 `vendor/office-layer/`：

```bash
PY="vendor/office-layer/.venv/bin/python"
$PY vendor/office-layer/scripts/docx_kit.py md --in draft.md --out 交付.docx
```

禁止：
- 用文本框/空格拼「伪表格」
- 手写 OOXML 绕过本层
- 生成 `.md` 就当交付物（除非用户明确只要 Markdown）

## 3. 不虚构

**这是本包最容易出事的地方，高校材料一旦编造后果严重。**

| 对象 | 规则 |
|---|---|
| 学生数据 | 绝不生成。只用用户提供的文件；缺则留占位并索要 |
| 文献引用 | 绝不编造作者/年份/期刊/DOI。用户给了才写，没给留 `【待补文献】` |
| 政策条文 | 不引具体条款号。只写「按学校/学院相关规定」并提示教师核对 |
| 个人事实 | 论文成果、项目编号、获奖名称等一律显式占位 |

占位格式统一用 `【待补：具体说明】`，便于检索与替换。

## 4. 一次性收集缺失信息

不要边做边问。开头的 intake 环节一次性把缺的信息列成清单问完
（沿用 awesome-benzi 的 `question-batch` 思路）：学科 / 课程名 / 学生层次 / 产出物格式 /
学校模板要求 / 时间节点。

信息不全时**用安全默认值继续**，并在交付物里标注何处用了默认值，而不是停下干等。

## 5. 人工确认位

涉及**评价、打分、结论、是否通过**的判断性内容，产出必须留显式人工修改位：

```
【待教师确认：是否同意答辩】
【待教师确认：分数 ____】
```

AI 只出草稿，不给终稿。这条在 `thesis-supervisor`、`exam-pipeline`、`peer-review` 里是红线。

## 6. 敏感数据

成绩、学号、姓名、身份证、联系方式属敏感信息：

- 只在本机处理，不上传、不外发
- 中间文件（转换产物、缓存）在任务结束后提醒教师删除
- 在交付说明里明确写出「本文件含学生个人信息，请注意保管」

## 7. SKILL.md 结构规范

```markdown
---
name: <kebab-case，与目录名一致>
description: "<中文说明>。Triggers: <至少 3 句教师真实会说的话>"
---

# <标题>
> Goal / 一句话说明这个 skill 解决什么

## 何时使用
## 前置信息收集（intake）
## 工作流（分阶段，每阶段有明确退出条件）
## 底座
## 产出物
## 红线
## 自检清单
```

**description 必须能被总控路由准确触发**，写进教师真实会说的口语短语，不要写抽象名词。

## 8. 自检清单（交付前逐项过）

- [ ] `description` 里至少有 3 个教师真实会说的触发短语
- [ ] 引用的 `vendor/` 路径**真实存在**（用 `ls` 验证过，不是凭记忆写的）
- [ ] 有「不虚构 + 一次性收集缺失信息」纪律
- [ ] 文件产出走了 `office-layer`
- [ ] 评价性内容留了人工确认位
- [ ] 总控 `SKILL.md` 路由表已更新
- [ ] 脚本实测跑通过（不是「应该能跑」）

## 9. 教师确认环节

产出初稿后必须明确告诉教师「这是初稿，请重点核对 X/Y/Z」，不要直接给终稿口吻。
